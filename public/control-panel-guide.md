# 📘 دليل لوحة التحكم — أكاديمي (كلية الهندسة المدنية / الجامعة التكنولوجية)

> دليل شامل للمشرف/المدير لاستخدام **لوحة التحكم** والتحكم الكامل بكافة أنظمة التطبيق دون الحاجة إلى كتابة أي كود.

---

## 🔐 1. الدخول إلى لوحة التحكم

- **المسار:** من الصفحة الرئيسية اضغط بطاقة **«لوحة التحكم»**، أو انتقل مباشرة إلى `/control-panel`.
- **كلمة المرور الافتراضية:** `2021` (يمكن تغييرها من داخل اللوحة نفسها — انظر القسم 3).
- **نصيحة أمنية:** غيّر كلمة المرور فور أول دخول، ولا تشاركها إلا مع من يحق له الإدارة الكاملة.

---

## 🧭 2. الأقسام الرئيسية في لوحة التحكم

اللوحة مقسّمة إلى صناديق (بطاقات) واضحة، كل صندوق يتكفّل بجانب من الإدارة:

| الصندوق | الوظيفة |
|---|---|
| 👥 **إدارة التدريسيين (المستخدمون)** | إضافة/حذف حسابات التدريسيين، تعيين الأقسام، إعادة تعيين كلمات المرور. |
| 🎨 **نمط الواجهة (10 ثيمات + وضع ليلي + 3D)** | اختيار الثيم العام، تفعيل الوضع الليلي، تشغيل/إطفاء التأثير ثلاثي الأبعاد. |
| 🔐 **كلمة مرور لوحة التحكم** | تغيير كلمة دخول اللوحة نفسها. |
| 🛡️ **كلمة مرور المدير (التكليفات الفردية)** | كلمة المرور المستخدَمة للعمليات الحسّاسة في نظام التكليفات. |
| 📦 **مجموعات الأنظمة** | تجميع عدة أنظمة في مجموعة واحدة تظهر كبطاقة في الصفحة الرئيسية. |
| 🧩 **منشئ الأنظمة (بدون كود)** | إنشاء نظام كامل جديد اعتماداً على جدول Google Sheets. |
| 🗂️ **رفع ملفات Google Drive** | رفع PDF/صور/مستندات من داخل أي نظام مُنشأ إلى مجلد Drive معروضة كأزرار تفاعلية. |
| 👁️ **إظهار/إخفاء الأنظمة + حماية بكلمة مرور** | التحكم برؤية كل نظام في الواجهة، وتفعيل قفل بكلمة مرور لكل نظام. |


---

## 👥 3. إدارة التدريسيين

- **إضافة تدريسي:** أدخل الاسم الكامل + القسم + كلمة مرور مبدئية (افتراضياً `123`).
- **تعديل:** غيّر القسم أو الاسم، أو أعد تعيين كلمة المرور.
- **حذف:** يُنقل التدريسي تلقائياً إلى ورقة `archive` كنسخة احتياطية.
- **تسجيل الدخول من قِبل التدريسي:** يتم من بطاقة **«التكليفات الفردية»** في الواجهة الرئيسية.

---

## 🎨 4. نمط الواجهة

- **10 ثيمات** رسمية بمستوى جامعي — اختر أيها بضغطة واحدة، يُطبَّق فوراً على جميع الشاشات.
- **الوضع الليلي 🌙 / النهاري ☀️:** زر واحد، يعمل مع أي ثيم.
- **الوضع ثلاثي الأبعاد 🟢 3D:** يفعّل تأثيرات العمق على بطاقات الواجهة.

---

## 🔐 5. كلمات المرور

- **كلمة مرور اللوحة:** لدخول لوحة التحكم فقط.
- **كلمة مرور المدير:** تُستخدم داخل نظام التكليفات الفردية للعمليات الحسّاسة (تعديل بيانات، حذف، …).
- **كلمة مرور كل نظام:** تُضبط من صندوق «إظهار/إخفاء الأنظمة» — انظر القسم 8.

> ⚠️ عند تغيير أي كلمة مرور، سيُطلب منك إدخال كلمة مرور اللوحة الحالية للتأكيد.

---

## 📦 6. مجموعات الأنظمة

تسمح بتجميع عدة بطاقات (مثلاً كل ما يخص «الإشراف») في بطاقة واحدة على الرئيسية.

**خطوات إنشاء مجموعة:**
1. اضغط **«➕ إضافة مجموعة»**.
2. أدخل: الاسم، الوصف، الأيقونة، اللون.
3. حدد الأنظمة المراد ضمّها بالنقر على مربع كل نظام.
4. اضغط **«💾 حفظ المجموعة»** — سيُطلب إدخال كلمة مرور اللوحة.
5. ستظهر المجموعة كبطاقة موحّدة في الصفحة الرئيسية، وعند دخولها يرى المستخدم قائمة الأنظمة المضمّنة.

**حذف مجموعة:** يعيد أنظمتها للظهور بشكل منفرد في الرئيسية.

---

## 🧩 7. منشئ الأنظمة بدون كود

أهم ميزة في اللوحة — تُنشئ نظاماً كاملاً بواجهة، فلاتر، طباعة، وصلاحيات، من مجرد جدول Google Sheets.

### 7.1 الحقول الأساسية
- **العنوان، الوصف، الأيقونة، اللون، ترتيب الظهور.**
- **مصدر البيانات:** إما جدول المشروع الحالي أو رابط Google Sheet خارجي.
- **معرّف الورقة (GID):** الأرقام في نهاية رابط الورقة داخل الشيت.
- **نطاق الأعمدة:** مثال `F:N` — الأعمدة التي ستظهر للمستخدم.

### 7.2 الفلاتر
لكل فلتر تحدد:
- **العمود** (بحرف Excel، مثل `F`).
- **نوع التحكم:**
  - `قائمة منسدلة (select)` — الاختيار من القيم الفريدة.
  - `بحث + قائمة (combo)` — قائمة مع بحث نصي.
  - `نص حر (text)` — كتابة كلمة للبحث.
  - **`نطاق رقمي (من — إلى)`** — لعرض الصفوف بين رقمين (مثلاً من 1 إلى 10). ✨
  - **`نطاق تاريخ (من — إلى)`** — لعرض الصفوف بين تاريخين. ✨
- **إلزامي؟** إن فُعّل، لا تظهر أي بيانات قبل اختيار قيمة (مفيد للفصل الدراسي مثلاً).
- **قواعد مسمّاة (rules):** خيارات جاهزة تظهر بجانب القيم (مثلاً: «أقل من 10»، «بين 5 و20»).

### 7.3 الشروط (Conditions) والأعمدة المشتقّة
- **الشروط:** لإخفاء صفوف لا تحقق شرطاً معيّناً (مثل: عمود `H` = "نظري").
- **AND / OR:** طريقة دمج الشروط.
- **الأعمدة المشتقّة:** إنشاء عمود جديد يظهر في الجدول بناءً على معادلة أو تحويل قيم.

### 7.4 صلاحيات CRUD (إضافة/تعديل/حذف/عرض)
لكل نظام حدد إن كان يسمح بـ:
- **عرض** الجدول الإداري.
- **إضافة** صف جديد إلى الشيت.
- **تعديل** صف قائم.
- **حذف** صف.

> عند التفعيل: أزرار **تعديل / حذف** تظهر مجاورة لكل صف، وأزرار **إضافة / بحث** في أعلى الجدول، **فقط للمستخدمين الذين لديهم الصلاحية**. من ليس لديه صلاحية لا يرى الأزرار أصلاً.

**أنواع الأعمدة في نموذج CRUD:**
- `text` / `number` / `date` / `select` / `readonly`.
- خيارات القائمة يمكن أن تكون **يدوية** أو **مستخرَجة تلقائياً من العمود**.
- **زر رابط:** إن حدّدت لعمود `column_link_labels` سيظهر زر «افتح الملف» بدلاً من عرض الرابط الخام.
- **📎 عمود ملف (رفع إلى Google Drive):** إن اخترت نوع العمود `file` يظهر زر رفع ملف في نموذج الإضافة/التعديل، ويُخزَّن رابط الملف تلقائياً في الخلية. انظر الشرح التفصيلي في القسم 7.8.


### 7.5 قفل الوصول للتدريسي
- **require_teacher_auth:** يفرض تسجيل دخول التدريسي.
- **teacher_column / teacher_department_column:** تحديد أعمدة الاسم والقسم.
- **نطاق التصفية:**
  - `name` — يرى صفوفه فقط.
  - `department` — يرى صفوف قسمه.
  - `name_or_department` — يفيد رؤساء الأقسام.
  - `all` — يرى الكل بعد تسجيل الدخول.

### 7.6 إعدادات الطباعة والمعاينة 🖨️
هذه من أهم الإضافات — تسمح لك بضبط المعاينة **مرّة واحدة** وقفلها للمستخدم.

| الخيار | الوصف |
|---|---|
| **الاتجاه** | Portrait / Landscape |
| **الحجم** | A4 / A3 / Letter |
| **الهامش** | 5 / 8 / 12 مم |
| **تكرار الرأس / التواقيع** | على كل صفحة |
| **إظهار الشعار / العنوان / معلومات الكلية / الفلاتر / التاريخ / رقم الوثيقة / العدّاد / التواقيع** | كل عنصر يُظهَر/يُخفى مستقلاً |
| **العلامة المائية (Watermark)** | نص «University of Technology» قطرياً — يمكن تخصيص النص أو إخفاؤه |
| **عنوان الطباعة الثابت (title)** | نص العنوان — يُقفل ولا يستطيع المستخدم تغييره |
| **إظهار شريط المعاينة (show_toolbar)** | إن أُطفئ، يختفي الشريط ويظهر بدلاً منه زر عائم ⚙️ فقط |
| **🔒 قفل الإعدادات (lock_settings)** | يُلغي زر الإعدادات العائم ويعطّل كل حقول الشريط — لا يستطيع المستخدم إظهار أو تعديل أي شيء |
| **✏️ استثناء «العنوان» من القفل (title_editable)** | يظهر فقط عند تفعيل القفل — يجعل حقل العنوان قابلاً للتعديل رغم قفل باقي العناصر |

**مثال عملي:** ضع اتجاه أفقي + هامش 8 + أخفِ الفلاتر والتاريخ + اقفل الإعدادات + استثنِ العنوان → المستخدم يستطيع فقط تغيير نص العنوان قبل الطباعة، وكل شيء آخر ثابت.

### 7.7 التواقيع
أضف حتى عدة تواقيع (اسم + مسمى وظيفي) تظهر في أسفل الطباعة.

---

## 🗂️ 7.8 رفع الملفات إلى Google Drive (عمود ملف تفاعلي)

هذه الميزة تسمح لأي نظام مُنشأ بدون كود باستقبال ملفات (PDF، صور، Word…) عند إضافة/تعديل سجل، ورفعها تلقائياً إلى مجلد محدّد في Google Drive، ثم تخزين رابط الملف في Google Sheets وعرضه كزر تفاعلي في الجدول.

### أولاً: الإعداد العام (مرة واحدة)
1. يجب أن يكون لدى المشرف **حساب Google** و**مشروع Google Cloud** مفعّل فيه Google Drive API.
2. يُنشئ OAuth 2.0 credentials من نوع **Web application**، ويحصل على:
   - `Client ID`
   - `Client Secret`
   - `Refresh Token`
3. تُخزَّن هذه القيم كأسرار آمنة في الخادم (Lovable Cloud) تحت الأسماء:
   - `GOOGLE_DRIVE_CLIENT_ID`
   - `GOOGLE_DRIVE_CLIENT_SECRET`
   - `GOOGLE_DRIVE_REFRESH_TOKEN`
4. تُعاد نشر دالة `drive-upload` بعد إضافة الأسرار.

> ⚠️ لا تُدخل هذه القيم في واجهة المستخدم أو في ملفات عامة — هي للخادم فقط.

### ثانياً: ضبط النظام من منشئ الأنظمة

#### الخطوة 1: فعّل صلاحيات الإضافة والتعديل
انتقل إلى تبويب **«5. الحماية والصلاحيات»** في منشئ الأنظمة، وتأكد من تفعيل:
- ➕ **إضافة** (إلزامي)
- ✏️ **تعديل** (اختياري — إن أردت استبدال الملف لاحقاً)

#### الخطوة 2: حدّد مجلد Google Drive الافتراضي
في نفس التبويب، ستجد حقل:

> **📁 فولدر Google Drive الافتراضي**

ألصق فيه أحد الأمثلة التالية:
```
https://drive.google.com/drive/folders/1ABC123xyz...
```
أو المعرّف المباشر:
```
1ABC123xyz...
```

هذا المجلد سيستقبل كل ملفات النظام إذا لم يُحدّد مجلد خاص للعمود.

#### الخطوة 3: أضف عمود ملف
1. انتقل إلى تبويب **«الأعمدة / الحقول»** (أو قسم إعدادات الأعمدة داخل CRUD).
2. اختر العمود الذي تريد أن يستقبل الملفات.
3. اضبط **نوع الحقل** على `file` (📎 ملف).
4. عند اختيار `file` تظهر لك حقول إضافية:
   - **📁 فولدر خاص بهذا العمود (اختياري):** إن أردت أن تذهب ملفات هذا العمود إلى مجلد مختلف عن المجلد الافتراضي للنظام.
   - **🔗 نص زر الرابط:** النص الذي يظهر في الجدول بدلاً من الرابط الخام، مثل:
     - `افتح الملف`
     - `عرض PDF`
     - `تحميل المستند`

> 💡 إن لم تُدخل فولدر خاص بالعمود، سيُستخدم فولدر النظام الافتراضي تلقائياً.

#### الخطوة 4: حدّد نص زر الرابط (اختياري لكن مُستحسن)
في حقل **«نصوص أزرار الروابط»** أو `column_link_labels`، أدخل قيمة مقابلة للعمود، مثلاً:
```json
{
  "H": "افتح الملف"
}
```
حيث `H` هو حرف العمود الذي اخترته نوع `file`.

### ثالثاً: كيف يستخدم المستخدم النظام

#### عند الإضافة
1. يدخل المستخدم إلى النظام ويضغط **«➕ إضافة سجل»**.
2. يملأ الحقول العادية.
3. في حقل الملف يظهر زر **«اختر ملفاً»**.
4. يختار الملف من جهازه (PDF، صورة، مستند Word…).
5. عند الضغط على **«حفظ»**:
   - يُرفع الملف إلى Google Drive في المجلد المحدّد.
   - يُمنح الملف صلاحية **«أي شخص لديه الرابط يستطيع العرض»**.
   - يُحفظ `webViewLink` (رابط الملف) في الخلية المناظرة في Google Sheets.

#### عند العرض
- في الجدول، تظهر الخلية كزر تفاعلي بالنص الذي حدّدته (مثل `افتح الملف`).
- عند الضغط على الزر يفتح الملف في تبويعة جديدة.
- إن لم تُضف نصاً للزر، يظهر الرابط الخام قابل للنسخ.

#### عند التعديل
- إن فعّلت صلاحية التعديل، يمكن للمستخدم استبدال الملف بآخر جديد.
- عند الحفظ يُرفع الملف الجديد ويُحدَّث الرابط في Google Sheets.

### رابعاً: القيود والنصائح

| القيد | التوضيح |
|---|---|
| **الحجم الأقصى** | 25 ميغابايت للملف الواحد (بسبب قيود نقل البيانات عبر Edge Function). |
| **أنواع الملفات** | يُقبل أي نوع: PDF, JPG, PNG, DOCX, XLSX, ZIP… |
| **التسمية** | يُفضّل تسمية الملفات بوضوح قبل الرفع؛ اسم الملف الأصلي يُحفظ في Drive. |
| **الأمان** | الملف يُصبح قابلاً للعرض لمن لديه الرابط. لا ترفع ملفات سرية حسّاسة. |
| **المجلدات** | يمكن لكل نظام مجلد افتراضي، ولكل عمود ملف مجلد خاص إن أردت التنظيم. |

### خامساً: مثال عملي — نظام «سجل المستندات الإدارية»

**الهدف:** نظام يستقبل مستندات إدارية (PDF) من كل قسم ويخزّنها في Drive.

1. أنشئ نظاماً جديداً في منشئ الأنظمة.
2. اضبط مصدر البيانات إلى Google Sheet يحتوي على الأعمدة:
   - `A`: رقم المستند
   - `B`: القسم
   - `C`: التاريخ
   - `D`: الموضوع
   - `E`: **الملف (PDF)**
3. في تبويب الصلاحيات:
   - فعّل **إضافة** و**تعديل**.
   - في حقل **«فولدر Google Drive الافتراضي»** ألصق رابط مجلد `مستندات إدارية`.
4. في إعدادات الأعمدة:
   - اجعل العمود `E` من نوع `file`.
   - اجعل نص زر الرابط `عرض المستند`.
5. احفظ النظام.
6. عند استخدامه: يضغط المستخدم **إضافة سجل**، يملأ البيانات، يختار ملف PDF، ويحفظ.
7. في الجدول يظهر زر **«عرض المستند»** بجانب كل سجل — يفتح الملف مباشرة.

### سادساً: حل المشاكل الشائعة

| المشكلة | السبب المحتمل | الحل |
|---|---|---|
| زر «اختر ملفاً» لا يظهر | لم تُفعّل صلاحية **إضافة** أو لم تضبط العمود على `file`. | راجع تبويب «الصلاحيات» وإعدادات الأعمدة. |
| الرابط لا يفتح | الملف لم يُرفع أو الرابط غير صحيح. | تأكد من صحة `GOOGLE_DRIVE_REFRESH_TOKEN` وأن Drive API مفعّل. |
| الملف يرفع لكن لا يظهر في الجدول | لم تُضف `column_link_labels` للعمود. | أضف نص الزر للعمود المقابل. |
| ظهور رسالة «Google Drive credentials are not configured» | الأسرار غير مضبوطة في الخادم. | أضف `GOOGLE_DRIVE_CLIENT_ID` و`GOOGLE_DRIVE_CLIENT_SECRET` و`GOOGLE_DRIVE_REFRESH_TOKEN`. |
| الملفات تذهب لمجلد واحد رغم ضبط مجلد للعمود | الفولدر الخاص بالعمود لم يُحفظ أو لم يُعاد فتح النظام. | أعد فتح النظام في المنشئ، تأكد من الفولدر، واحفظ مرة أخرى. |

---

### 7.8 اسم الرابط بالإنكليزية (URL slug)

- الحقل في الخطوة 1 من المنشئ. يظهر في رابط الوصول: `/custom/<slug>`.
- يقبل فقط حروفاً إنكليزية صغيرة، أرقام، `-`، `_`.
- ⚠️ تغييره لنظام قائم يكسر الروابط المحفوظة.

### 7.9 📸 الاستخراج بالذكاء الاصطناعي (OCR)
- من الخطوة 5 فعّل «📸 استخراج تلقائي من الصور».
- يظهر زر **📷 استخراج من صورة** في نموذج «إضافة سجل» ليقوم النموذج بتعبئة الحقول تلقائياً من صورة/وثيقة/بطاقة.
- يعتمد على Lovable AI (Gemini) — لا حاجة لأي مفتاح خارجي.
- يمكن تخصيص التعليمات (prompt) وحصر الأعمدة المستهدفة بأحرف Excel.

### 7.10 ✨ الميزات المتقدمة (الخطوة 8)

- **🔍 شريط بحث عام:** مربع فوق الجدول يفلتر السجلات فوراً عبر جميع الأعمدة الظاهرة.
- **🎨 قواعد تلوين الصفوف:** لكل قاعدة اختر لوناً + شرطاً واحداً أو أكثر (AND/OR). أول قاعدة يتحقق شرطها تُطبّق لونها على خلفية الصف — مثالي لتمييز «المتأخر»، «الحرج»، «المكتمل».
- **📊 ذيل المجاميع:** أضف عموداً واحداً أو أكثر لعرض:
  - `Σ المجموع` — للأعمدة الرقمية.
  - `x̄ المتوسط` — بمعامل عشري.
  - `# عدد القيم` / `#∪ عدد القيم الفريدة`.
  - `↓ الأصغر` / `↑ الأكبر`.
  - يُحسب تلقائياً على الصفوف بعد التصفية والبحث.

---

## 🧠 7-B. جولة تفصيلية داخل «منشئ الأنظمة» — خطوة بخطوة

المنشئ يفتح كحوار مقسّم إلى **8 خطوات (تبويبات)** في الأعلى. لا يجب إكمالها بالترتيب؛ يمكنك التنقل بينها بحرّية، والحفظ في أي لحظة من زر **«💾 حفظ النظام»** أسفل الحوار.

### الخطوة 1 — الأساسيات
- **العنوان بالعربية:** يظهر في بطاقة الرئيسية وأعلى صفحة النظام (مثال: «سجل حضور التدريسيين»).
- **الوصف:** سطر شرح قصير أسفل عنوان البطاقة.
- **الأيقونة:** رمز إيموجي واحد (📋 📊 📅 🧾 …).
- **اللون:** لون البطاقة (Hex — مثال `#0891b2`).
- **ترتيب الظهور:** رقم صحيح، الأصغر يظهر أوّلاً في الرئيسية.
- **URL slug (بالإنكليزية):** يحدّد الرابط `/custom/<slug>` (مثال: `attendance-log`). لا تغيّره بعد التوزيع؛ يكسر الروابط المحفوظة.

### الخطوة 2 — المصدر والأعمدة
- **مصدر البيانات:**
  - **جدول المشروع الحالي:** يستعمل الشيت الرئيسي المنشور — لا تحتاج رابطاً.
  - **جدول Google Sheet خارجي:** ألصق الرابط الكامل (`https://docs.google.com/spreadsheets/d/…/edit`). يجب أن يكون **مشاركاً للجميع كقارئ** أو منشوراً على الويب.
- **معرّف الورقة (GID):** الرقم بعد `#gid=` في رابط التبويب المستهدف داخل الشيت.
- **نطاق الأعمدة:** مثل `F:N` — كل الأعمدة الظاهرة للمستخدم. لا يشمل أعمدة الفلترة فقط، بل كل ما تريد عرضه.
- **تسميات الرؤوس (اختياري):** أعد تسمية أي عمود ليظهر بعنوان أفضل من الموجود في الشيت.

### الخطوة 3 — الفلاتر
- زر **«➕ إضافة فلتر»** يضيف صفاً جديداً.
- لكل فلتر: **حرف العمود** + **نوع التحكم** + **تسمية اختيارية** + **إلزامي؟** + **قواعد مسمّاة** (اختياري).
- ✅ الفلاتر المتعددة تعمل بمنطق **AND** (كل الفلاتر الفعّالة يجب أن تنطبق).

### الخطوة 4 — الشروط والأعمدة المشتقّة
- **الشروط:** يظهر النظام فقط الصفوف التي تحقق كل الشروط (أو أحدها إذا اخترت OR). أمثلة: `H = "نظري"`، `M >= 3`.
- **الأعمدة المشتقّة:** أنشئ عموداً جديداً مبنياً على عمليات (concat, if, min/max بين عمودين …). العمود المشتقّ يظهر في الجدول والفلاتر تماماً كأي عمود أصلي.

### الخطوة 5 — الحماية والصلاحيات
- **حماية بكلمة سر:** كلمة مرور فقط لدخول هذا النظام (منفصلة عن كلمة اللوحة).
- **تفعيل النظام:** إخفاء/إظهار البطاقة من الرئيسية.
- **اشتراط دخول التدريسي:** يفعّل شريط تسجيل الدخول العلوي. اضبط بعدها **عمود الاسم**، **عمود القسم**، و**نطاق التصفية** (name / department / name_or_department / all).
- **صلاحيات CRUD:** فعّل ما تريد إتاحته (عرض / إضافة / تعديل / حذف). العمليات تكتب مباشرة في شيت Google وتتطلب كلمة مرور اللوحة.
- **📷 OCR:** يضيف زر «استخراج من صورة» في نموذج الإضافة.

### الخطوة 6 — الأزرار السريعة (Quick Filters)
- أزرار قابلة للتبديل (Toggle) فوق الجدول تُطبّق فلتراً جاهزاً بضغطة واحدة.
- لكل زر: تسمية + أيقونة + لون + عمود + عملية (equals / contains / gt / between …) + قيمة.
- مثال: زر «🔴 غير مستوفي» يفلتر الصفوف التي `Q < 10`.

### الخطوة 7 — إعدادات الطباعة والمعاينة
راجع الجدول التفصيلي في القسم 7.6 أعلاه. باختصار: اضبط كل شيء مرة واحدة، ثم فعّل **قفل الإعدادات** ليصبح كل ما يراه المستخدم جاهزاً وموحّداً.

### الخطوة 8 — الميزات المتقدمة
- **🔍 بحث عام** + **🎨 قواعد تلوين الصفوف** + **📊 ذيل المجاميع** (تفاصيل في 7.10).

---

## 🧪 7-C. أمثلة تطبيقية

### مثال 1 — نظام بسيط: «دليل غرف الكلية»
الهدف: عرض جدول ثابت للغرف مع فلتر واحد وطباعة أنيقة.

| الخطوة | الإعداد |
|---|---|
| 1. الأساسيات | العنوان: «دليل غرف الكلية» — الأيقونة: 🏛️ — اللون: `#0f766e` — slug: `rooms` |
| 2. المصدر | جدول المشروع الحالي — GID: `123456789` — نطاق: `B:F` |
| 3. الفلاتر | فلتر واحد على العمود `C` (الطابق) نوعه **select** |
| 4. الشروط | لا شيء |
| 5. الصلاحيات | تفعيل النظام فقط — بلا كلمة مرور، بلا CRUD |
| 6. أزرار سريعة | لا شيء |
| 7. طباعة | Portrait — A4 — تكرار الرأس ✔ — قفل الإعدادات ✔ |
| 8. متقدمة | 🔍 بحث عام ✔ |

**النتيجة:** بطاقة في الرئيسية، جدول بسيط، فلتر طابق، بحث فوري، طباعة موحّدة.

---

### مثال 2 — نظام متوسط: «سجل حضور التدريسيين»
الهدف: كل تدريسي يرى صفوفه فقط، ويستطيع إضافة سجل حضور جديد بصورة (OCR).

| الخطوة | الإعداد |
|---|---|
| 1. الأساسيات | العنوان: «سجل الحضور» — 🕒 — slug: `attendance` |
| 2. المصدر | GID: `987654321` — نطاق: `A:H` |
| 3. الفلاتر | فلتر **نطاق تاريخ** على العمود `B` (التاريخ) — إلزامي ✔ |
| 4. الشروط | لا شيء |
| 5. الصلاحيات | ✔ اشتراط دخول التدريسي — عمود الاسم: `C` — نطاق: `name` — ✔ إضافة — ✔ عرض — ✔ OCR |
| 6. أزرار سريعة | «🟢 حاضر» → عمود `E` = "حاضر" — «🔴 غائب» → `E` = "غائب" |
| 7. طباعة | Landscape — إخفاء الفلاتر — عنوان ثابت: «كشف الحضور الشهري» |
| 8. متقدمة | 🎨 قاعدة: إذا `E = "غائب"` → خلفية `#fee2e2` — 📊 ذيل: عدد على `A` |

**النتيجة:** كل تدريسي يدخل، يفلتر بشهر، يرى فقط أيامه، يضيف يوماً جديداً برفع صورة استمارة الحضور فيملأ النظام الحقول تلقائياً.

---

### مثال 3 — نظام معقّد: «متابعة مشاريع التخرّج»
الهدف: نظام إشراف بصلاحيات مركّبة، تلوين حالات، تجميع، وطباعة رسمية مقفلة.

| الخطوة | الإعداد |
|---|---|
| 1. الأساسيات | العنوان: «متابعة المشاريع» — 🎓 — slug: `projects-track` |
| 2. المصدر | جدول خارجي — GID: `55555` — نطاق: `A:Q` — تسميات: `A→"رقم"`, `F→"المشرف"`, `N→"النسبة%"` |
| 3. الفلاتر | `F` (المشرف) — combo + بحث • `H` (القسم) — select — إلزامي ✔ • `N` (النسبة) — نطاق رقمي • `K` (التاريخ) — نطاق تاريخ |
| 4. الشروط | `Q ≠ "مؤرشف"` — منطق AND — عمود مشتقّ: `الأيام المتبقية` = فرق بين `L` واليوم |
| 5. الصلاحيات | ✔ اشتراط دخول التدريسي — عمود الاسم: `F` — عمود القسم: `H` — نطاق: **name_or_department** — ✔ عرض ✔ تعديل — بلا حذف — كلمة سر نظام: نعم |
| 6. أزرار سريعة | «⚠️ متأخرة» → «الأيام المتبقية» < 0 • «🟡 قاربت» → between 0 و 7 |
| 7. طباعة | Landscape — A3 — تكرار الرأس + التواقيع ✔ — علامة مائية «University of Technology» — 3 تواقيع (المشرف، رئيس القسم، العميد) — قفل الإعدادات ✔ — استثناء العنوان ✔ |
| 8. متقدمة | 🔍 بحث عام ✔ — 🎨 3 قواعد: «متأخرة» أحمر، «قاربت» أصفر، «مكتمل ≥ 95%» أخضر — 📊 ذيل: `Σ` على `N`، `x̄` على «الأيام المتبقية»، `#∪` على `F` |

**النتيجة:** رئيس القسم يرى كل مشاريع قسمه، المشرف يرى مشاريعه فقط، تلوين تلقائي للحالات الحرجة، تجميع فوري في الأسفل، وطباعة موحّدة لا يستطيع المستخدم تشويهها — فقط تعديل العنوان.

---





---

## 👁️ 8. إظهار/إخفاء الأنظمة وحمايتها

لكل نظام (سواء افتراضي أو مُنشَأ من المنشئ):
- **مرئي / مخفي** — يختفي من الرئيسية بالكامل.
- **محمي بكلمة مرور** — يطلب كلمة مرور عند الدخول.
- **العنوان المعروض / الوصف / الأيقونة / اللون** — قابلة للتخصيص من هنا مباشرة.
- **ترتيب الظهور (sort_order)** — رقم أصغر = يظهر أوّلاً.

---

## 💾 9. الحفظ والمزامنة

- كل عملية حفظ تطلب كلمة مرور اللوحة.
- الإعدادات تُحفظ على الخادم (Lovable Cloud) ويتم مزامنتها لجميع المستخدمين خلال ثوانٍ.
- تُحدَّث الواجهة تلقائياً عند تغيير الإعدادات دون الحاجة لإعادة تحميل الصفحة.

---

## 🆘 10. حل المشاكل الشائعة

| المشكلة | الحل |
|---|---|
| «كلمة مرور خاطئة» عند الحفظ | تأكد من كلمة مرور اللوحة الحالية (وليس كلمة مرور نظام آخر). |
| النظام الجديد لا يظهر | تأكد أن `enabled = true` وأنه ليس مخفياً في «إظهار/إخفاء». |
| الفلتر لا يعمل | تحقق من حرف العمود (Excel letter) ومن أن العمود يقع ضمن `columns_range`. |
| المستخدم يرى بيانات ليست له | فعّل `require_teacher_auth` واضبط `teacher_column` و«نطاق التصفية». |
| زر «⚙️ الإعدادات» يظهر رغم القفل | تأكد أن `lock_settings = true` (وليس فقط `show_toolbar = false`). |
| العنوان لا يُعدَّل بعد القفل | فعّل «✏️ استثناء العنوان من القفل». |

---

## 🗂️ 11. فهرس الأنظمة الحالية ومصدر بيانات كل نظام

> جميع الأنظمة تقرأ مباشرةً من **ملف Google Sheets الرسمي للكلية** (نفس المصنّف المنشور)، وكل نظام مرتبط بورقة (Sheet) واحدة أو أكثر داخل هذا المصنّف. الجدول أدناه يوضّح: العنوان في الواجهة، المسار (Route)، اسم ورقة العمل التي تغذّيه، ومعرّف الورقة (GID) للرجوع السريع.

### 11-A. الأنظمة الأساسية للجدول الدراسي

| # | النظام | المسار | ورقة العمل | GID |
|---|---|---|---|---|
| 1 | 👨‍🏫 **جدول التدريسي** | `/teacher` | `Schedule` (الجدول الأصلي) | `0` |
| 2 | 🎓 **جدول الطلبة** | `/student` | `الجدول حسب القسم واليوم` | `1765483005` |
| 3 | 🕓 **تتبع الاسبوع الحالي** | `/tracking` | مشتق من `Schedule` (نفس ورقة جدول التدريسي) | `0` |
| 4 | 🏫 **القاعات الفارغة** | `/empty-rooms` | مشتق من `Schedule` | `0` |
| 5 | 📋 **التكليفات** | `/assignments` | `التكليفات` | `1416068353` |
| 6 | 📊 **الإحصائيات والمخططات** | `/charts` | مُجمّع من `Schedule` + `التكليفات` + `الساعات` + `النصاب` | — |
| 7 | 🧾 **ملخص الأخطاء** | `/errors` | مُجمّع من مخرجات أنظمة التدقيق الأربعة أدناه | — |

### 11-B. أنظمة التدقيق (Audit)

كلمة المرور الافتراضية لهذه الأنظمة: `ahmed1` (تُضبط من صندوق «إظهار/إخفاء الأنظمة»).

| # | النظام | المسار | ورقة العمل | GID |
|---|---|---|---|---|
| 8 | 🗂️ **بوّابة أنظمة التدقيق** | `/audit` | صفحة عنوان تجميعية لكل تقارير التدقيق أدناه | — |
| 9 | 📅 **تدقيق الجدول الدراسي** | `/audit-report` | `Schedulereport` | `587741649` |
| 10 | ⏱️ **تدقيق الساعات الدراسية** | `/audit-hours` | `الساعات` | `1878774467` |
| 11 | 🎯 **تدقيق نوع المحاضرة** | `/audit-lecture-type` | مشتق من `Schedule` (يرصد الصفوف التي لم يُحدَّد فيها نظري/عملي) | `0` |
| 12 | 📝 **تدقيق تكليفات القسم** | `/audit-assignments` | `التكليفات` | `1416068353` |
| 13 | ⚖️ **تدقيق النصاب** | `/quota-audit` | `النصاب` | `457825033` |

### 11-C. أنظمة الإشراف والدراسات العليا

| # | النظام | المسار | ورقة العمل | GID |
|---|---|---|---|---|
| 14 | 🎓 **تقرير الإشراف** | `/supervision-report` | `الإشراف` (SUPERVISION) | `567847712` |
| 15 | ⌛ **الإشراف المنتهي** | `/expired-supervision` | نفس ورقة `الإشراف` (تصفية بالتاريخ بعد 1 أيلول) | `567847712` |
| 16 | 🧑‍🎓 **طلبة من دون مشرف** | `/students-without-supervisor` | `الدراسات العليا` (تصفية العمود I = 0) | `345813260` |
| 17 | 🎓 **طلبة الدراسات العليا في مرحلة البحث** | `/research-phase-students` | `الدراسات العليا` | `345813260` |
| 18 | 📐 **سقف الإشراف** | `/supervision-cap` | `فحص سقف الإشراف` (CHECK) | `997769481` |
| 19 | 📁 **المشاريع** | `/projects` | `المشاريع` (PROJECT) | `2145658694` |
| 20 | 🎓 **طلبة المرحلة الرابعة** | `/fourth-stage-students` | `طلبة المرحلة الرابعة` (STUDENTS) | `1210995176` |
| 21 | 📝 **تدقيق تكليفات مشاريع التخرج** | `/projects-assignments-audit` | نفس ورقة `طلبة المرحلة الرابعة` | `1210995176` |
| 22 | ⚖️ **أعباء الإشراف الكلية** | `/supervision-workload` | `فحص الأعباء الكلية` (CHECKALLHR) | `1081297434` |
| 23 | 🚧 **تجاوز سقف مشاريع التخرج** | `/project-supervision-exceeded` | نفس ورقة `فحص الأعباء الكلية` | `1081297434` |
| 24 | 📚 **تدريسيون بلا محاضرات نظرية** | `/teachers-without-theory` | نفس ورقة `فحص الأعباء الكلية` | `1081297434` |
| 25 | 🧑‍🏫 **تدريسيون بلا إشراف** | `/unassigned-supervisors` | نفس ورقة `فحص الأعباء الكلية` | `1081297434` |

### 11-D. الأنظمة الإدارية والفردية

| # | النظام | المسار | مصدر البيانات |
|---|---|---|---|
| 26 | 👤 **التكليفات الفردية (استمارة الأجور)** | `/individual-assignments` | ورقة `استمارة الاجور` — GID `1147039908` — بالإضافة إلى ورقتَي `users` و `archive` (للمصادقة والأرشفة، تُدار عبر Edge Function `sheet-auth`). |
| 27 | ⚙️ **لوحة التحكم** | `/control-panel` | إعدادات تُخزَّن في قاعدة بيانات Lovable Cloud (وليس في Google Sheets). |
| 28 | 🧩 **الأنظمة المُنشأة بدون كود** | `/custom/<slug>` | كل نظام يحدد فيه المشرف مصدره الخاص: إمّا نفس مصنّف المشروع أو رابط Google Sheet خارجي + GID + نطاق أعمدة. |

### 11-E. ملاحظات مهمّة حول مصادر البيانات

- **مصنّف واحد رسمي:** كل الأنظمة الافتراضية (1 → 26) تقرأ من نفس مصنّف الكلية المنشور على Google Sheets — يكفي تحديث الأوراق داخله لتحديث الواجهة تلقائياً خلال 60 ثانية (أو فوراً عند الضغط على 🔄).
- **الأنظمة المشتقّة** (تتبع، القاعات الفارغة، تدقيق نوع المحاضرة، الإحصائيات، ملخص الأخطاء) لا تعتمد على ورقة مستقلة، بل تُحسب على الطاير من الأوراق الأصلية.
- **الأنظمة المشتركة في الورقة نفسها** (مثل 14–15، أو 22–25) تختلف فيما بينها بالفلاتر والشروط المطبَّقة على نفس البيانات — أي تعديل في الورقة ينعكس عليها جميعاً.
- **صلاحية الوصول للأوراق:** يجب أن تبقى الأوراق منشورة (Publish to web) بصيغة CSV ليتمكّن التطبيق من قراءتها.
- **ورقة الأرشيف (`archive`)** خاصة بنظام التكليفات الفردية فقط — تُنقل إليها حسابات التدريسيين المحذوفين تلقائياً.

---

## 📞 12. الدعم

- **الإشراف:** الأستاذ الدكتور علي مجيد خضير الدهوي — عميد كلية الهندسة المدنية.
- لأي طلب تعديل جوهري أو نظام جديد معقّد، تواصل مع فريق التطوير.

---

*آخر تحديث: 2026*
